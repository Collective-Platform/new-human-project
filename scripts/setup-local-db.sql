-- Local Development Database Setup
--
-- This file is intentionally near-empty.
--
-- The `nhp` Postgres schema and all NHP tables are created by
-- `drizzle-kit migrate`. Run that after `createdb new_human_dev` and
-- you're done.
--
-- This file remains as a placeholder so existing tooling (e.g. the
-- `db:setup-local` npm script) keeps working without error.

SELECT 1;
